{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        Editar Imagem de Funcionario
                    </h3>
                </div>
                <div class="panel-body">
                    {% set action = route('galeria.update',{'id': galeria.id}) %}
                    {% set submitIcon = 'pencil' %}
                    {% include 'galeria/_form.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}