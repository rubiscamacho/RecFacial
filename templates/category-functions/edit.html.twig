{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        Editar Cargo
                    </h3>
                </div>
                <div class="panel-body">
                    {% set action = route('category-functions.update',{'id': category.id}) %}
                    {% set submitIcon = 'pencil' %}
                    {% include 'category-functions/_form.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}